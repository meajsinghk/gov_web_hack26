Role: Principal Security Auditor & GovTech Performance EngineerAudit, verify, and enhance the EPFO Member Dashboard codebase (login.html, dashboard.html, dashboard.css, claim-engine.js, mock-backend.js).Your task is to conduct an exhaustive technical review focusing on deterministic validation, algorithmic accuracy, accessibility, and low-bandwidth resilience.Task 1: Audit the Pre-Flight Jaro-Winkler String Distance AlgorithmInspect the name matching logic in claim-engine.js:Verify the exact mathematical implementation of the Jaro-Winkler metric:$$d_j = \frac{1}{3} \left( \frac{m}{\vert{}S_1\vert{}} + \frac{m}{\vert{}S_2\vert{}} + \frac{m - t}{m} \right)$$$$d_w = d_j + \ell p (1 - d_j)$$Ensure prefix scaling constant $p = 0.1$ and maximum prefix length $\ell \le 4$.Verify test cases:Case A (Exact Match): "RAMESH KUMAR" vs "RAMESH KUMAR" -> Score = 1.0 (Direct Approval).Case B (Minor Transposition/Prefix): "RAMESH KUMAR" vs "RAMESH K" -> Score $\approx 0.88$ (Demographic OTP Reconciliation Flow).Case C (Divergent Identity): "RAMESH KUMAR" vs "SITA DEVI" -> Score $< 0.40$ (Deterministic Rejection with clear, non-cryptic explanation).Task 2: GIGW 3.0 & WCAG 2.1 AA Accessibility ValidationVerify that all modal dialogs (Claim Drawer, Aadhaar OTP Modal) implement full keyboard focus trapping (focus cannot escape the modal until closed or completed).Confirm that every dynamic status update (such as pre-flight checks or balance re-calculations) uses aria-live="polite" or aria-live="assertive" so screen readers announce changes immediately.Ensure all interactive touch targets meet the minimum physical size of $44 \times 44$ pixels for mobile accessibility.Task 3: Low-Bandwidth & Offline Resilience TestVerify that mock-backend.js and claim-engine.js utilize local state caching (localStorage / sessionStorage) so that if a user loses cellular connectivity on a 3G connection midway through filing a claim, their inputs and uploaded state are preserved without data loss.Confirm that all CSS and JS assets contain zero external CDN dependencies (fonts, icons, or stylesheets) and operate completely offline in a sandbox environment.Task 4: Statutory Scheme Rule Engine VerificationAudit the programmatic rule engine in claim-engine.js to ensure 100% fidelity to the Employees' Provident Funds Scheme, 1952:Form 31 (Medical Advance - Para 68J): Must permit partial withdrawal up to 6 months of basic wages or employee share with interest, without requiring an employer exit date.Form 19 (Final Settlement): Must require date_of_exit to be present and non-null. If missing, block submission with an actionable link to "Mark Exit Date".Form 10C (Pension Withdrawal): Permissible only if total_service_months is less than 120 months (10 years). If $\ge 120$ months, display an alert explaining that the user is entitled to a Monthly Pension (Form 10D) instead of a lump-sum withdrawal.Output the audited, fully integrated, and corrected code files directly.